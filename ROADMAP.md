# Roadmap

## 上下文能力

- 启用旧章节摘要召回：基于当前章节、续写要求、知识库关键词，从较早章节摘要中选择相关内容进入 Prompt。
- 在续写页增加上下文控制：最近原文字数、旧章节召回数量、知识库数量等可配置。
- 支持固定或排除上下文来源：让用户手动指定某些章节摘要或知识库条目本次必带或不带。
- 在上下文预览中展示每个模块的来源、命中关键词和字符数，方便排查模型写偏原因。

## 摘要与知识维护

- 增加章节摘要批处理：为缺摘要章节批量生成摘要，支持中断和进度显示。
- 接受续写后提示更新当前章节摘要或全书摘要，避免正文推进后摘要滞后。
- 增加知识库条目模板：角色、地点、势力、世界观规则使用不同字段提示。
- 支持从章节正文中辅助提取角色、地点、关键线索候选，用户确认后写入知识库。

## 项目数据与版本

- 增加项目 JSON 导出和导入，完整保存章节、摘要、知识库、配置之外的项目状态和生成历史。
- Electron 版支持选择项目文件并自动保存，降低浏览器 localStorage 丢失风险。
- 生成候选正文保留未接受记录，支持恢复上一次候选或基于候选继续改写。
- 增加接受生成后的简单回滚能力，允许撤销最近一次追加或新增章节。

## LLM 与生成体验

- 增加 LLM 配置预设，例如 DeepSeek、OpenAI 兼容接口、Ollama、本地代理。
- 增加生成前健康检查：提示全书摘要为空、当前章摘要缺失、续写要求为空、知识库未命中等风险。
- 支持自定义输出长度和生成模式，例如续写、润色、改写、补对白、补场景描写。
- 增加非流式兼容模式，适配不支持 Server-Sent Events 的 OpenAI 兼容服务。

## 编辑体验

- 支持章节新增、删除、拆分、合并和拖拽排序。
- 在章节列表增加搜索和筛选，例如缺摘要、字数异常、标题关键词。
- 增加字数目标和章节进度统计，便于长篇规划。
- 为大文本编辑器增加快捷保存状态、字数显示和更清晰的焦点样式。
